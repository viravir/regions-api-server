// TODO -> add tests for controller
/**
 * Pre-each:
 * - Create mock repository
 * - Create mock service on base of injected mock repository
 * - Inject mock service into controller
 * Test cases (each):
 * - Validation works
 * - Service method get called
 * - Errors are handled
 * Test cases (individual):
 * - .get returns 404 if region does not exist
 */